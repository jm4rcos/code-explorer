export const DEFAULT_LOGIN_REDIRECT = '/';

export const publicRoutes = ['/', '/community', '/not-found', '/not-found'];

export const protectedRoutes = ['/snippets', '/snippets/[id]'];

export const authRoutes = ['/auth/reset', '/auth/login', '/auth/register', '/auth/error', '/auth/new-password'];
